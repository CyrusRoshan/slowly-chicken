{
  "name": "slowly-chicken",
  "version": "0.0.1",
  "description": "Chicken, but slowly.",
  "private": true,
  "scripts": {
    "build": "rm -r dist; tsc --build && cp manifest.json ./dist && cp -r ./assets ./dist",
    "dev": "nodemon --watch ./src --ext ts --exec yarn build"
  },
  "author": "Cyrus Roshan",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.200",
    "nodemon": "^2.0.20",
    "typescript": "^4.8.4"
  }
}
